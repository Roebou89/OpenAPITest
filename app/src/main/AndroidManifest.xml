<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.roebou.openapitest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_casino"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_casino"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TransactionActivity"></activity>
        <activity android:name=".SettingsActivity" />
        <activity android:name=".AddOrganisationActivity" />
        <activity android:name=".LoginActivty" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>